apiVersion: batch/v1
kind: Job
metadata:
  name: group-purchase-reindex-job
  labels:
    app: batch
    component: job
    job-type: reindex
spec:
  backoffLimit: 1
  activeDeadlineSeconds: 1800
  ttlSecondsAfterFinished: 86400
  template:
    metadata:
      labels:
        app: batch
        job-type: reindex
    spec:
      automountServiceAccountToken: false
      nodeSelector:
        kubernetes.io/hostname: batch-server
      restartPolicy: Never
      containers:
      - name: batch-job
        image: minbros/batch:latest
        imagePullPolicy: Always
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: "prod"
        - name: TZ
          value: "Asia/Seoul"
        - name: SPRING_BATCH_JOB_NAME
          value: "groupPurchaseReindex"
        - name: JOB_DESCRIPTION
          value: "공구 ES 인덱스 재생성 작업"
        - name: INDEX_ALIAS
          value: "group-purchase"
        - name: MODE
          value: "full"
        - name: SPRING_KAFKA_BOOTSTRAP_SERVERS
          value: "kafka-service:9092"
        - name: ELASTICSEARCH_URIS
          value: "http://elasticsearch-service:9200"
        envFrom:
          - secretRef:
              name: batch-secret
        resources:
          requests:
            memory: "1.5Gi"
            cpu: "2000m"
            ephemeral-storage: "2Gi"
          limits:
            memory: "2Gi"
            cpu: "3500m"
            ephemeral-storage: "3Gi"
